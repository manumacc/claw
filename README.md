# claw

`claw` is a small Rust assistant runtime with a daemon, chat CLI, provider listing, and local chat management.

## Usage

```sh
just daemon
just chat "hello"
just providers
```


## Development

```sh
just check
```
